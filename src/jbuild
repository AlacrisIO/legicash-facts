;; jbuilder (dune) file for Legicash sources

(jbuild_version 1)

(executable
 ((name test_file)
  (preprocess (pps (ppx_deriving.std)))
  (libraries (tezos-stdlib tezos-crypto cryptokit secp256k1))))
